# @package _global_

# Experiment: Weight Averaging (Incremental)
# Uses WeightAveragingRunning class from wildllamas/merge.py

output_dir: "outputs/experiment/weight_averaging"

merge:
  method: weight_averaging_running
